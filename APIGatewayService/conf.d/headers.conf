# TODO: Review all security headers and update for production-grade deployment.
# TODO: Set a strict and specific Content-Security-Policy.
# TODO: Add Referrer-Policy (e.g. no-referrer or strict-origin-when-cross-origin).
# TODO: Add Strict-Transport-Security (HSTS) once HTTPS is enabled.
# TODO: Evaluate and set Permissions-Policy for browser features.
# TODO: Add Cache-Control and Pragma headers as appropriate for API/data responses.
# TODO: Review and limit CORS headers for production; do not use '*' in allow_origins.
# TODO: Add/remove headers to avoid disclosing sensitive server information (e.g., Server header).
# TODO: Consider removing X-Powered-By or similar headers if present.
# TODO: Validate necessity/effectiveness of X-XSS-Protection; consider modern browser support.
add_header X-Content-Type-Options nosniff;
add_header X-Frame-Options SAMEORIGIN;
add_header X-XSS-Protection "1; mode=block";
